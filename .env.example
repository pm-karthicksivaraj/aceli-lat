# ─── PostgreSQL (Neon) ───────────────────────────────────────
# Sign up at https://neon.tech → create a project → copy connection strings
#
# DATABASE_URL  — pooled connection (Vercel runtime uses this)
#   Format: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require
#
# DIRECT_URL    — direct connection (Prisma migrations use this)
#   Format: postgresql://user:pass@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require
# ─────────────────────────────────────────────────────────────
DATABASE_URL=
DIRECT_URL=
